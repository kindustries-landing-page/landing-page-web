name: Deploy Klotus Landing Page Web Production
on:
  push:
    branches: [ master ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          umask 077
          if printf '%s' "${{ secrets.DEPLOY_KEY }}" | base64 -d > ~/.ssh/id_ed25519 2>/dev/null; then
            echo "Loaded DEPLOY_KEY as base64"
          else
            printf '%s\n' "${{ secrets.DEPLOY_KEY }}" > ~/.ssh/id_ed25519
            echo "Loaded DEPLOY_KEY as raw private key"
          fi
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H 100.75.67.115 >> ~/.ssh/known_hosts
      - name: Deploy Web on Head
        run: |
          ssh -i ~/.ssh/id_ed25519 lio@100.75.67.115 '
            set -e
            cd /opt/repos/klotus/landing-page-web
            git fetch origin
            git checkout master
            git reset --hard origin/master
            cd /opt/stacks/klotus-landing-page-web
            docker compose up -d --build --force-recreate
            for i in $(seq 1 30); do
              code=$(docker exec klotus-landing-page-web sh -lc "wget -qO- http://127.0.0.1:3000/ >/dev/null 2>&1; echo $?" || true)
              if [ "$code" = "0" ]; then
                echo "Web health OK (container localhost /)"
                exit 0
              fi
              sleep 2
            done
            echo "Web health check failed"
            exit 1
          '
